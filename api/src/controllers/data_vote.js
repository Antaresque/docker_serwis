const axios = require('axios');
const DATA_URI = process.env.DATA_SERVICE_URI;

const { handleErrors } = require('../helper');

async function addForImage(id){

}
async function removeForImage(id){
    
}
async function addForComment(id){
    
}
async function removeForComment(id){
    
}

// --------------- VOTES ----------------------
module.exports = {
    addForImage, removeForImage, addForComment, removeForComment
}